apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jumia_phone_validator.fullname" . }}-test-connection"
  labels:
    {{- include "jumia_phone_validator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jumia_phone_validator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
