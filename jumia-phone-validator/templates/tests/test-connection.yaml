apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jumia-phone-validator.fullname" . }}-test-connection"
  labels:
    {{- include "jumia-phone-validator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jumia-phone-validator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
