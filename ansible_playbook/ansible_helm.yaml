---
  - name: Helm Installing Jumia-Phone-Validator MicroService to EKS in AWS
    hosts: localhost
    tasks:
      - name: Installing Jumia-Phone-Validator MicroServices
        register: unameout
        command: "helm install jumia-phone-validator --generate-name"
      
      - debug:
          var: unameout.stdout_lines 